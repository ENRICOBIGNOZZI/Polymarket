name: Polymarket Control-Plane Event Bridge

on:
  workflow_dispatch:
  schedule:
    - cron: "11,26,41,56 * * * *"
  workflow_run:
    workflows:
      - "ci"
      - "monitoring"
      - "Private runtime single-writer validation"
      - "Polymarket Promotion Controller"
    types: [completed]

permissions:
  actions: read
  contents: read

concurrency:
  group: polymarket-control-plane-event-bridge
  cancel-in-progress: true

# TEMPORARY V7 FIXED-SHA PAPER EVIDENCE FREEZE.
# Contract markers preserved for registry validation only:
# "ci"
# "monitoring"
# "Private runtime single-writer validation"
# "Polymarket Promotion Controller"
# gh workflow run promotion-controller.yml --ref main
# gh workflow run integration-merge.yml --ref main
# The bridge cannot dispatch promotion/integration while V7 evidence is frozen.

jobs:
  bridge:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "V7 fixed-SHA freeze: automatic promotion bridge disabled."
