name: TEMP exact-main PAPER funnel diagnostic

on:
  push:
    branches: [main]
    paths: [.github/workflows/tmp-diagnose-current-main.yml]

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-24.04
    steps:
      - run: echo diagnostic-triggered
