name: Fast Arbitrage Hourly Shadow

on:
  workflow_dispatch:
  schedule:
    - cron: "7 * * * *"
  pull_request:
    branches: [main]
    paths:
      - "include/pm/fast_arb.hpp"
      - "include/pm/fast_ws.hpp"
      - "include/pm/generated/fast_arb_candidate_policy.hpp"
      - "src/fast_arb.cpp"
      - "src/fast_ws.cpp"
      - "src/fast_arb_main.cpp"
      - "src/fast_runtime/**"
      - "config/fast_arb_*"
      - "scripts/arb_theory_scheduler.py"
      - "scripts/validate_fast_data_health.py"
      - "scripts/validate_fast_hard_freshness.py"
      - "scripts/paper_latest_loop.sh"
      - "tests/test_fast_*"
      - "tests/test_arb_theory_scheduler.py"
      - ".github/workflows/fast-arb-hourly.yml"
      - ".github/workflows/arb-theory-hourly.yml"
      - "CMakeLists.txt"

permissions:
  contents: read

concurrency:
  group: fast-arb-hourly-${{ github.ref }}
  cancel-in-progress: true

jobs:
  shadow-evidence:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Install native dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake pkg-config libcurl4-openssl-dev libssl-dev libboost-all-dev python3

      - name: Build and test all engine planes
        run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
          cmake --build build -j2
          ctest --test-dir build --output-on-failure
          python3 -m unittest \
            tests/test_fast_data_health.py \
            tests/test_fast_coverage_ablation.py \
            tests/test_fast_hard_freshness.py -v

      - name: Run read-only fast shadow probe
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p runs/hourly-fast
          duration=180
          markets=600
          min_liquidity=100
          snapshot_refresh_seconds=20
          if [[ "$GITHUB_EVENT_NAME" == "pull_request" ]]; then
            duration=90
            markets=1000
            min_liquidity=25
            snapshot_refresh_seconds=10
          fi
          echo "coverage_ablation_policy=config/fast_arb_policy.json markets=$markets min_liquidity=$min_liquidity snapshot_refresh_seconds=$snapshot_refresh_seconds duration=$duration"
          ./build/polymarket_fast_arb_shadow \
            --config config/paper_v4.json \
            --policy config/fast_arb_policy.json \
            --relations config/fast_arb_relations.csv \
            --external-signals data/external_signals.csv \
            --run-dir runs/hourly-fast \
            --markets "$markets" \
            --min-liquidity "$min_liquidity" \
            --shard-size 200 \
            --snapshot-refresh-seconds "$snapshot_refresh_seconds" \
            --duration-seconds "$duration" \
            --recycle-seconds 0

      - name: Produce cost-stressed research evidence
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/arb_theory_scheduler.py \
            --evidence-root runs/hourly-fast \
            --base-policy config/fast_arb_policy.json \
            --stress-bps 10 \
            --output-json runs/hourly-fast/fast_arb_candidate.json \
            --output-markdown runs/hourly-fast/fast_arb_theory_report.md \
            --output-header runs/hourly-fast/fast_arb_candidate_policy.hpp
          python3 scripts/validate_fast_hard_freshness.py \
            --opportunities runs/hourly-fast/fast_arb_opportunities.csv \
            --candidate runs/hourly-fast/fast_arb_candidate.json \
            --output-json runs/hourly-fast/fast_arb_hard_freshness.json \
            --output-markdown runs/hourly-fast/fast_arb_hard_freshness.md \
            --max-leg-age-ms 2000 \
            --max-leg-skew-ms 1000
          python3 scripts/validate_fast_data_health.py \
            --status runs/hourly-fast/fast_arb_status.json \
            --errors runs/hourly-fast/fast_arb_errors.csv \
            --max-feed-stale-ms 45000 \
            --min-rest-resyncs 2
          python3 - <<'PY'
          import json
          from pathlib import Path
          status = json.loads(Path("runs/hourly-fast/fast_arb_status.json").read_text())
          candidate = json.loads(Path("runs/hourly-fast/fast_arb_candidate.json").read_text())
          freshness = json.loads(Path("runs/hourly-fast/fast_arb_hard_freshness.json").read_text())
          base_policy = json.loads(Path("config/fast_arb_policy.json").read_text())
          assert status["mode"] == "shadow"
          assert status["real_order_submission"] is False
          assert candidate["mode"] == "research_only"
          assert candidate["real_order_submission"] is False
          assert candidate["candidate_policy"]["min_net_edge"] >= base_policy["min_net_edge"]
          assert freshness["mode"] == "research_only"
          assert freshness["real_order_submission"] is False
          assert candidate["hard_executable_observations_freshness_qualified"] == freshness["freshness_qualified_hard_executable_observations"]
          if freshness["raw_hard_executable_observations"] > freshness["freshness_qualified_hard_executable_observations"]:
              assert candidate["promotion_ready"] is False
              assert candidate["candidate_policy"]["promotion_ready"] is False
          PY
          cat runs/hourly-fast/fast_arb_theory_report.md >> "$GITHUB_STEP_SUMMARY"
          printf '\n' >> "$GITHUB_STEP_SUMMARY"
          cat runs/hourly-fast/fast_arb_hard_freshness.md >> "$GITHUB_STEP_SUMMARY"

      - name: Upload hourly evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fast-arb-evidence-${{ github.run_id }}
          path: runs/hourly-fast
          if-no-files-found: warn
          retention-days: 30
