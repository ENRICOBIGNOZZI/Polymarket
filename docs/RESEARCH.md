# V7 evidence

Economic evidence is collected only inside the two live PAPER algorithms. Zero-authority crypto contexts and component diagnostics never become separate algorithm families and cannot own capital, OMS, inventory or ledger authority.

Research artifacts are evaluated from causal forward data. The fair-value model is trained explicitly and then frozen for inference; the Maker execution model adapts only from the current PAPER run. Statistical evidence is reported separately from technical authority.
