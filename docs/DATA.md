# V7 data

The canonical data plane contains Polymarket books/trades, settlement oracle and external crypto venues, verified structural relations, decisions, PAPER orders/fills, inventory, risk and fair snapshots required by the two algorithms.

The append-only ledger is SHA-bound. Retention checkpoints immutable ledger bytes and prunes only bounded telemetry after durable archival.
