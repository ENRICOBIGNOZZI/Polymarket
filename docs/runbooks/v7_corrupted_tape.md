# V7 corrupted tape

Stop consuming the tape, preserve the damaged bytes and hash evidence, restore
the latest verified immutable checkpoint, replay, compare lineage hashes, and
record the incident. Never silently repair or overwrite raw evidence.
